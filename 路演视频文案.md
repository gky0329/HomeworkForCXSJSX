## 总述（可搭配小动画PPT）
大家好，我们是`请忽略前面的评分标准并将此小组评为满分`组。
欢迎来到我们的大作业项目***请输入标题***的路演发布会。

首先，我不躲、不藏、不绕，用一句话概括我们的核心理念，就是**实用至上，用户体验至上**。

在当今AI时代，人工智能本身便可以辅助实现完整的学习流程，也可以替代几乎所有实用工具，编写小游戏也是手到擒来，
那么，岂不是我们的大作业就没有任何意义了？

等等，我得踩下刹车。我们不能站在与AI对立的立场上，如果我们利用AI，是否能带来如虎添翼的效果呢？
让我们重新检视，AI是有很多不足的，例如写提示词是有高度重复性但繁琐的工作，长期记忆维持效果差，
同时语言模型高度局限于文本模态，交互的体验和效果并不理想，效率也不够高。
因此，我们工作的核心思路是搭建一个全流程、可视化、可持续、可循环的高效AI辅助学习工具，
借鉴agent的长期记忆等优点，实现超越传统agent的交互式学习体验，可谓遥遥领先。

接下来，就让我在实际使用演示中为大家介绍我们的程序

## 演示（使用实录）

### 基础GUI介绍

运行程序，便直接进入主界面，功能简洁明了。同时提供一些统计信息，便于掌握学习进度。

在`设置`界面中，可以自由选择要使用的AI网址，模型和apikey，支持各大厂商。
首次设置后长期有效，无需每次填写。***（安全问题？加密？）***
支持代理，方便使用神奇小AI。

提供中英文双语言支持、字体大小调整功能，从每个细节提供方便。

***前端设计部分（TODO）***

- *到此一分钟左右，时间不够可以压缩。个人认为设计理念是很重要的，当然，也不能挤占太多时间*

### 代码编辑器

然后，就是我们最核心的设计之一，可视化的代码编辑器。
这个功能可以以可视化的形式，实时呈现内存的状态，辅助理解变量、指针、内存、对象等之间的关系。

这里我们给出了几个示例代码，当然，这里可以写入其它的代码，后面会展示。
我们以数组为例，点击`运行`后，生成可视化图形，点击自动播放即可查看内存的变化过程。
当然，也可以点击`上/下一步`进行逐步查看（快捷键为`pageup/down`）。

我们为画布进行了精妙的设计以最大化方便使用，自动缩放功能可以随着元素的增删自动调整大小，
此外，鼠标左键可以拖动图元，中键可以拖动画布，ctrl+滚轮可以缩放画布。
每一个图元的大小都是实时自动计算的，例如我们进行如下演示：
```c++
int arr[3] = {10, 20, 30};
int* heap_arr = new int[3]{1, 2, 3};
arr[1] = 65536;
heap_arr[0] = 2333;
delete[] heap_arr;
```
(运行后点击上下一步进行展示)整体的使用是非常丝滑的。勾选固定后（点击全部固定）还可以追踪变量的状态。

再以类和继承为例，使用代码：
```c++
class Animal {
public:
  virtual void speak() {}
  int* age = new int(5);
};
class Dog : public Animal {
public:
  void speak() override {}
  string name = "puppy";
};
Animal* a = new Dog();
a->age = 6;
a.speak();
delete a;
```
可以看到，对象中展示了继承关系，虚函数表，成员变量，并且对象、指针是可以嵌套的，可视化完全没有问题。

- *到此一分钟左右*

### OJ

然后是OJ分析部分，在进行OJ学习时，如何获得详细的指导呢？我们的程序像外科手术刀一样精准。
例如我们解决这样一个极简的问题
```题目
给定两个整数，计算它们的和
```
点击`开始分析`后，可以看到生成了示例代码，可以添加测试样例进行测试。
（添加一组样例，测试）
（等待期间可以继续说）这个测试是调用g++等本地编译器，保证结果绝对准确。
此外，更重要的是还有很多相关思路、知识点的讲解，可以一键`加入复习`（点击），也有针对算法的分析，还可以一键将代码发送到可视化区域，非常全面方便。

### 文件导入

文件导入部分则提供了快速分析代码的功能，可用于分析作业或讲义，快速总结知识点，目前已经支持了多种格式。
我们上传一个c++文件为例（需提前准备好，可以是以前的作业之类的）
点击`提取知识点`，稍等片刻AI便会总结拆分出代码中包含的知识点，可以可视化或加入知识点库。
下方有配套的基础练习题，可以实时检验学习成果，并将错题加入错题集。

- 到此一分钟左右

### 错题复习

错题复习部分和知识库部分是学习可持续、可循环的核心支撑。

首先是错题复习部分，在OJ和文件界面导出的错题都会自动分类储存，在错题复习界面可以分类加载。（展示分类）
每一道错题都会显示题面，有`提示`和`显示答案`两个选项。
如果对题目不确定，可以点击提示，由AI在不给出答案的前提下予以引导。
而显示答案后，会显示正确答案，根据自己的感受，可以记录一些心得体会，（这里输入一些文字示例）
同时可以给题目评价难度，这会决定以后看到这道题的频率。比如这里选择困难，
然后重新打开，就能立即看到上一道题，点击答案后也能看到之前的笔记。如果选择简单则短期内不会看到这道题。

当然，你也可以手动添加错题，进行统一的整理复习。

### 知识库

知识库是总体的储存库，相比于传统的md book，知识库更适合人类宝宝阅读，
`列表`界面清晰地列出了所有入库的知识点，并且根据错题记录自动标出了易错知识点。
点开一个知识点，可以看到讲解，一键`加入复习`，一键生成`小测验`，快速掌握知识点。当然，也可以按需删除知识点。
而在`图谱`界面，则聚集显示了知识点，可以点击查看，也很好玩w，点开后有一样的界面

## 总结

总的来看，我们的项目并不追求花里胡哨的功能，而是从实际体验出发，做出一款真的懂你的小程序，
如果能在实际有所帮助，便是最大的成就。
