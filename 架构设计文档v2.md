# C++ 内存可视化学习工具 — 架构设计文档

> 版本 v1.0 | 2026-05-15 | 供组内评审
>
> 本文档定义新版产品的功能模块、技术架构和实现计划。**已废弃 MC 闯关路线。**

---

## 一、产品定位

### 1.1 为什么 pivot

| 问题 | 旧方案（MC 闯关） | 新方案（内存可视化） |
|---|---|---| 
| 与学习工作流匹配 | 闯关游戏和 PDF/课件/OJ 脱节 | 直接处理用户的课件和题目 |
| 版权风险 | MC 素材有版权隐患 | 可以减少外部素材依赖 |
| 关卡内容瓶颈 | 需要自己设计教学内容 | **AI 从用户上传的课件中自动提取教学内容** |
| 泛化能力 | 只能用在我们的项目里 | 理论上可拓展到其他编程语言甚至其他学科 |

### 1.2 一句话描述

**一个可自由塑造的 C++ 内存可视化沙盒**，通过用户主动定义的「画布元素」观察代码运行时细微的变化，**以极强的灵活性和可塑性**满足所有关键需求，同时结合 AI 实现对 PDF 课件和 OJ 题目的智能处理。

---

## 二、功能模块

### 2.1 📄 PDF 课件智能处理

```
PDF 上传 → 文本提取 → DeepSeek API →
  ├─ 知识点 JSON（概念名 + 解释 + 代码片段）
  ├─ 自动生成选择题（RAG 匹配往年题风格）
  ├─ 代码片段可一键「可视化运行」
  └─ 内容同步到错题复习库和知识图谱
```

**核心设计**：课件自带的选择题直接提取；生成题采用 RAG 思路——从用户上传的往年题中检索相似题型，让 AI 模仿出题风格，而非凭空生成。

### 2.2 💻 OJ 题目可视化讲解

```
用户粘贴 OJ 题面 → DeepSeek 分析 →
  ├─ 自然语言讲解（逐步解释代码执行过程中内存的变化）
  ├─ 关联知识点标注（指针、引用、STL 容器...）
  ├─ 代码片段可「可视化运行」
  └─ 内容和错误记录同步到错题库和知识图谱
```

### 2.3 🔍 变量追踪器（WYSIWYG）

```
代码运行 → 所有变量列在侧栏 →
  ├─ 拖拽变量到 Canvas → 持久显示追踪卡片
  ├─ 追踪卡片显示：变量名、类型、当前值、所属作用域
  ├─ 代码重新执行 → 追踪卡片自动刷新
  ├─ 作用域退出 → 卡片渐隐标记「已销毁」
  └─ 多变量对比视图
```

**核心设计**：所见即所得。用户选感兴趣的变量拖到画布上持续观察，代码每改一次、执行一次，追踪卡片自动更新。概念上类似 IDE 的 Watch 窗口，但可视化更强。

### 2.4 📝 Anki 式错题复习

```
错误记录 → 本地 JSON 存储（知识点 + 时间 + 上下文） →
  ├─ 复习列表（按错误频次排序）
  ├─ 手动标记错题
  ├─ [后续] SM-2 间隔重复算法
  └─ 数据驱动知识图谱生成
```

### 2.5 🧠 考前知识图谱

```
聚合数据：错误频率 + 已学知识点 + 课件章节结构 →
  ├─ 非对称频次加权：错得多的知识点节点更大、更红
  ├─ 依赖关系边（如「指针」依赖「取地址 &」）
  ├─ 力导向图绘制（QGraphicsView）
  └─ 一键生成个性化复习计划
```

---

## 三、UI 布局

```
┌──────────────────────────────────────────────────────────────┐
│  Tab: [📄 课件导入] [💻 OJ 解析] [📝 错题] [🧠 图谱] [⚙]  │
├──────────────────────────────┬───────────────────────────────┤
│                              │                               │
│   代码编辑区                 │     内存可视化 Canvas          │
│   (QPlainTextEdit)          │     (QGraphicsView)            │
│                              │                               │
│   int a = 42;               │  ┌Stack (main)─┐ ┌Heap─────┐ │
│   int* p = &a;              │  │ a : 42      │ │[0xA1]   │ │
│   *p = 100;  ← 步进执行     │  │ p : int* ───┼┼→ 100    │ │
│                              │  └─────────────┘ └─────────┘ │
│                              │                               │
├──────────────────────────────┴───────────────────────────────┤
│  🔍 变量追踪器: [a (int)=100] [p (int*)=0xA1] [+]          │
│  ┌────────────────────┐ ┌────────────────────┐              │
│  │ 追踪: a            │ │ 追踪: p            │              │
│  │ 类型: int          │ │ 类型: int*         │              │
│  │ 当前值: 100        │ │ 当前值: 0xA1       │              │
│  │ 作用域: main       │ │ 指向: [0xA1] → 100 │              │
│  └────────────────────┘ └────────────────────┘              │
└──────────────────────────────────────────────────────────────┘
```

**视觉风格**：深色背景（类似 VS Code Dark+），栈区蓝色矩形，堆区橙色圆角块，指针箭头灰色实线（悬挂时红色虚线），所有元素纯几何绘制，无任何外部贴图素材。

---

## 四、技术架构

### 4.1 分层架构

```
┌─────────────────────────────────────────────┐
│                  UI Layer                    │
│  ┌───────────┐  ┌──────────┐  ┌──────────┐ │
│  │ PDF 导入页 │  │ OJ 解析页 │  │ 知识图谱 │ │
│  └─────┬─────┘  └────┬─────┘  └────┬─────┘ │
│        │             │              │       │
│  ┌─────┴─────────────┴──────────────┴─────┐ │
│  │         Code Editor + Canvas           │ │
│  │  ┌─────────────┐  ┌────────────────┐   │ │
│  │  │ QPlainText  │  │ Memory Canvas  │   │ │
│  │  │   Edit      │  │ (QGraphicsView)│   │ │
│  │  └──────┬──────┘  └───────┬────────┘   │ │
│  │         │                 │             │ │
│  │  ┌──────┴─────────────────┴───────┐    │ │
│  │  │       Variable Tracker        │    │ │
│  │  └───────────────────────────────┘    │ │
│  └───────────────────────────────────────┘ │
└─────────────────────┬───────────────────────┘
                      │
┌─────────────────────┴───────────────────────┐
│               Core Engine                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ Step     │  │ Memory   │  │ StateDiff │  │
│  │ Executor │──│ Model    │──│ Generator │  │
│  └────┬─────┘  └──────────┘  └────┬─────┘  │
│       │                           │         │
│  ┌────┴───────────────────────────┴─────┐  │
│  │         Memory Model                 │  │
│  │  StackFrames[]  HeapBlocks[]         │  │
│  │  PointerEdges[] (to_stack/to_heap)   │  │
│  └──────────────────────────────────────┘  │
└─────────────────────┬───────────────────────┘
                      │
┌─────────────────────┴───────────────────────┐
│               Services                       │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐│
│  │ AI       │ │ PDF      │ │ Error Storage││
│  │ Service  │ │ Service  │ │ Service      ││
│  └──────────┘ └──────────┘ └──────────────┘│
└──────────────────────────────────────────────┘
```

### 4.2 MemoryModel 核心数据结构

```python
@dataclass
class Variable:
    name: str
    type: str
    value: Any
    is_pointer: bool = False

@dataclass
class StackFrame:
    name: str              # "main", "foo()"
    variables: list[Variable]

@dataclass
class HeapBlock:
    id: str                # "0xA1" 形式的地址标签
    type: str              # "int", "int[10]", ...
    value: Any
    is_freed: bool = False

@dataclass
class PointerEdge:
    from_var: str          # 指针变量名
    from_frame: str        # 所在栈帧
    to_heap_id: str | None        # 指向堆块（→指针→堆）
    to_stack_var: str | None      # 指向栈变量（→指针→栈）
    to_stack_frame: str | None    # 目标栈变量所在帧
    is_dangling: bool = False     # 目标已被释放

class MemoryModel:
    stack: list[StackFrame]
    heap: list[HeapBlock]
    edges: list[PointerEdge]
    current_frame_name: str
```

### 4.3 StepExecutor 支持的语句（MVP）

| ID | 语句 | 操作 |
|---|---|---|
| 1 | `int a = 42;` | 在 current_frame 创建 Variable |
| 2 | `int* p;` | 创建 is_pointer=True 的 Variable |
| 3 | `p = &a;` | 创建 PointerEdge（to_stack） |
| 4 | `*p = 100;` | 沿 edge 找到目标，修改 value |
| 5 | `p = new int(5);` | 创建 HeapBlock + PointerEdge（to_heap） |
| 6 | `delete p;` | 标记 HeapBlock.is_freed=True |
| 7 | 作用域进出 | `{` 创建新 StackFrame，`}` 销毁帧 + 泄漏检测 |

### 4.4 Canvas 渲染布局

```
┌────────────────────────────────────────────┐
│  Stack Area (左 40%)    Heap Area (右 60%) │
│                                            │
│  ┌──────────────┐      ┌────────────┐     │
│  │ main()       │      │ [0xA1]     │     │
│  │ ┌──────────┐ │      │ type: int  │     │
│  │ │ a:  42   │ │      │ value: 5   │     │
│  │ │ p: int* ─┼─┼──────┼→           │     │
│  │ └──────────┘ │      └────────────┘     │
│  └──────────────┘                         │
└────────────────────────────────────────────┘
```

### 4.5 变量追踪器

```
┌─ 变量追踪器 ──────────────────────────────────┐
│ 栅格布局 (FlowLayout / QGridLayout)            │
│                                                │
│  ┌─ a ───────────┐  ┌─ p ───────────┐         │
│  │ 类型: int     │  │ 类型: int*    │         │
│  │ 当前值: 100   │  │ 当前值: 0xA1  │         │
│  │ 作用域: main  │  │ → [0xA1]: 5  │         │
│  └───────────────┘  └───────────────┘         │
└────────────────────────────────────────────────┘
```

---

## 五、目录结构

```
CXSJSXhomework/
├── main.py
├── config.yaml
├── requirements.txt                 # PySide6 + pymupdf + pyyaml
├── app/
│   ├── core/
│   │   ├── memory_model.py          # StackFrame, HeapBlock, PointerEdge, MemoryModel
│   │   ├── step_executor.py         # C++ 语句解析 + 内存语义执行
│   │   ├── state_diff.py            # StateDiff 通信协议
│   │   └── engine.py                # Engine 封装
│   ├── ui/
│   │   ├── main_window.py           # 极简 Shell
│   │   ├── pages/
│   │   │   ├── home_page.py         # Dashboard（Tab 容器）
│   │   │   ├── code_page.py         # 代码编辑 + Canvas + 变量追踪
│   │   │   ├── pdf_page.py          # PDF 上传 + AI 处理
│   │   │   ├── oj_page.py           # OJ 题目粘贴 + AI 讲解
│   │   │   ├── review_page.py       # 错题复习
│   │   │   └── graph_page.py        # 知识图谱
│   │   ├── canvas/
│   │   │   ├── memory_canvas.py     # QGraphicsView 主画布
│   │   │   ├── stack_item.py        # 栈帧图形项
│   │   │   ├── heap_item.py         # 堆块图形项
│   │   │   ├── pointer_edge.py      # 指针箭头
│   │   │   ├── tracker_panel.py     # 变量追踪器面板
│   │   │   └── canvas_animator.py   # 动画编排
│   │   ├── widgets/
│   │   │   └── ai_help_dialog.py    # AI 助手对话框
│   │   └── theme/
│   │       ├── colors.py            # 全新配色（深色主题，非 MC）
│   │       └── styles.py            # 全局样式
│   └── services/
│       ├── ai_service.py            # DeepSeek API 调用
│       ├── pdf_service.py           # PDF 文本提取（pymupdf）
│       └── error_store.py           # 错题本地 JSON 存储
├── data/
│   ├── user/                        # 用户数据（错题、设置）
│   └── kb/                          # RAG 知识库
└── tests/
    └── unit/
```

---

## 六、实现 Sprint 计划

### Sprint 1：核心引擎 + Canvas（MVP 基石）
- MemoryModel（StackFrame/HeapBlock/PointerEdge）
- StepExecutor（7 种内存语句解析）
- MemoryCanvas（QGraphicsView 栈/堆/指针可视化）
- CodePage（代码编辑器 + Canvas + 一键运行）

### Sprint 2：PDF 课件处理
- PDF 文本提取（pymupdf）
- AI 知识点提取 + 选择题生成
- RAG 往年题风格匹配
- PDF 页面 UI（上传 → 处理 → 结果展示）

### Sprint 3：OJ 题目 + 变量追踪
- OJ 题面粘贴 + AI 讲解
- 变量追踪器（拖拽 + 持久卡片）
- Canvas 动画（飞入/闪烁/渐隐）

### Sprint 4：错题复习 + 知识图谱
- 错题本地存储 + 列表
- 知识图谱力导向图绘制
- 频次加权 + 依赖边
- 复习卡片生成

---

## 七、关键技术决策

| # | 决策 | 理由 |
|---|---|---|
| 1 | Canvas 用纯几何图形（矩形+箭头），不用任何贴图 | 零版权风险，干净的技术感 |
| 2 | 深色主题（VS Code Dark+ 风格） | 符合程序员审美，降低视疲劳 |
| 3 | AI API key 由用户自行提供 | 不承担 API 费用，用户自主控制 |
| 4 | 错误存储本地 JSON | 离线可用，隐私友好 |
| 5 | 知识图谱用 QGraphicsView 力导向图 | 不引入 D3.js 等外部渲染依赖 |
| 6 | 不使用 AST，正则匹配 C++ 子集+AI处理 | MVP 可快速交付，后续可替换 tree-sitter |
